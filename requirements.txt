
# E-Commerce Fraud Detection — Python Dependencies


# ── Core Data Stack ────────────────────────────────────────────────────────────
pyspark==3.5.1
pandas>=2.0.0,<3.0.0
numpy>=1.26.0,<2.0.0
pyarrow>=14.0.0          # Parquet I/O for PySpark ↔ Pandas bridge

# ── Machine Learning ──────────────────────────────────────────────────────────
scikit-learn>=1.4.0,<2.0.0
lightgbm>=4.3.0
xgboost>=2.0.0            # Optional — included for completeness per spec

# ── Experiment Tracking ────────────────────────────────────────────────────────
mlflow>=2.12.0
boto3>=1.34.0             # MLflow artifact store on S3

# ── Configuration & Environment ───────────────────────────────────────────────
pyyaml>=6.0
python-dotenv>=1.0.0

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0           # Optional — nice default themes

# ── Streaming (Task 7) ────────────────────────────────────────────────────────
kafka-python>=2.0.2
confluent-kafka>=2.3.0    # Higher-performance C-extension Kafka client

# ── Data Validation ───────────────────────────────────────────────────────────
great-expectations>=0.18.0   # Schema & quality gates
jsonschema>=4.21.0           # schema.json validation

# ── Workflow Orchestration (Airflow DAG deps) ─────────────────────────────────
# apache-airflow is installed separately via the Airflow Docker image.
# List only the extra providers used in DAG code:
apache-airflow-providers-common-sql>=1.12.0

# ── Testing & CI ──────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.4.0               # Fast linter (replaces flake8 + isort)
black>=24.0.0             # Auto-formatter

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.66.0              # Progress bars for batch jobs
requests>=2.31.0          # HTTP calls (health checks, webhooks)
